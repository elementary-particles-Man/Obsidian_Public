
現状の「AutoMateテストの運用ルール」以下に整理して再提示します。

---

## ✅ 【AutoMate JSON出力ルール】

1. **1入力に対して、必ずJSONは1件のみ出力**
検証結果及び進捗は従来通り。JSON以降は出力禁止。
2. **生成するJSONの構造**
"D:\Dev\KAIRO\logs\work_results.txt"はAutoMateで削除するため、
従来通り「必要なログがあるなら別ファイルで保存して最後リネーム」
特に完了だけ受け取りたいなら作成指示

３．自動実行中の判断

プロンプトが[AutoMate] で始まる場合は自動運用
